	
function.displayRadioValue(){ ariavaluemax
    var elementos=document.getElementById('game');
    for (i=0; i<elementos.clientHeight; i++){
        if(elementos [i] checked){
            document.getElementById(result).innerHTML
            o jogados selecionado e +elements [i].value
        }
    }
}

function getoption (){ariavaluemax
    let selectelement=document.querySelector(select1)
    let output=selectelement.value
    document.querySelector(output.textontert=output)
}

function getcheckbox(){ariavaluemax
let getcheckbox=document.querySelectorAll('input[type="checkbox"]')
let text=''
   for(i=0; i<getcheckboxer.length; i++){
    text=`${text} ${getcheckboxer[i].value}`
    document.getElementById.apply(pri).innerHTML=text
   }
}
function getElementById(){
    text= `erro`
}